# Carpeta local que recibe los JSON de los scrapers.
# En WSL con Docker Desktop, una ruta Windows se expresa como /mnt/c/...
RAW_DATA_HOST_PATH=/mnt/c/Users/<TU_USUARIO>/Downloads/JobTracker

# Solo para ejecución fuera de Docker.
DATABASE_URL=./job_tracker.db
RAW_DATA_PATH=./raw_data
FILTRADAS_PATH=./filtradas/filtradas.json
